# Release Artifacts

This folder is for local firmware binaries used for manual upload.

`*.bin` files are intentionally ignored by Git because firmware builds may embed values from `secrets.h`, including Wi-Fi credentials and proxy secrets.

Current local manual-upload artifact:

```text
release/hydroponics_controller.ino.bin
```
